{
  "name": "jobnok",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "dev:local": "concurrently -n docker,web -c yellow,blue \"docker compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.local.yml up --build\" \"pnpm --filter jobnok-frontend dev\"",
    "dev:api": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build postgres api worker",
    "prod": "docker compose up --build -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "api:migrate": "docker compose exec api alembic upgrade head",
    "api:migrate:new": "docker compose exec api alembic revision --autogenerate -m",
    "api:migrate:down": "docker compose exec api alembic downgrade -1",
    "api:migrate:current": "docker compose exec api alembic current",
    "build": "pnpm --filter jobnok-frontend build",
    "lint": "pnpm --filter jobnok-frontend lint",
    "typecheck": "pnpm --filter jobnok-frontend type-check"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
