# packages/api-client

Reserved for a future typed client generated from the `apps/api` OpenAPI schema. Not populated by the `apps/api`/`apps/web` reorg migration — `apps/web` still calls the API directly via `fetch`.
